#include <mympf/mympf.h>

namespace mympf {

float_t add(const float_t &x, const float_t &y) {
  float_t z;
  // size_t s = std::max(x.decimal_part.number.size(),
  //                     y.decimal_part.number.size());
  // mympz::bignum_t decimal_part = mympz::add(x.decimal_part, y.decimal_part);
  // mympz::bignum_t decimal_part_carry;
  // if (decimal_part.number.size() > s) {
  //   decimal_part_carry = 
  // }
  return z;
}

} // namespace mympf